import React from "react";

// Use the `certificates` array here to dynamically render the certificate items.
const Certificates = () => {
  return (
    <section id="certificates" className="container my-5">
      <div>
        <h2 className="section-title">Certificates</h2>
        {/* Render certificates dynamically here */}
      </div>
    </section>
  );
};

export default Certificates;
