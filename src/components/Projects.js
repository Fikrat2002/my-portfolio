import React from "react";

// Use the `projects` array here to dynamically render the project items.
const Projects = () => {
  return (
    <section id="projects" className="container my-5">
      <div>
        <h2 className="section-title">Projects</h2>
        {/* Here we can render a carousel for projects */}
      </div>
    </section>
  );
};

export default Projects;
